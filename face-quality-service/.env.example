# Stash Face Quality Service - Environment Configuration

# Docker network name (must match your Stash/Compreface network)
DOCKER_NETWORK=stash-compreface

# External access port for the quality service
# Default: 6001 (change if port is already in use)
WEB_SERVICE_PORT=6001

# Internal service port (container port, typically no need to change)
INTERNAL_PORT=6001

# Temporary directory for file processing
QUALITY_SERVICE_TEMP=/tmp/quality_service
